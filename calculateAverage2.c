#include <stdio.h>

int calculateAverage(int array[])
{
	int i,sum,num,average,grades;
	for(i=0;i<num;++i)
	{
       	sum=sum+grades;
	}
	average=sum/num;
}
int main()
{
	int num,grades,i,average,sum;
	int array[]={num,grades};
	printf("Please enter the number of students :");
	scanf("%d",&num);
	printf("%d. student's grade: ",i+1);
	scanf("%d", &grades);
	printf("average = %d",calculateAverage(array));	

}
